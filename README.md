# SUTT-Project
Python program to parse a provided Excel spreadsheet containing the mess menu and converting its contents into Python objects. This is followed by the Python objects being serialized into a JSON file.
